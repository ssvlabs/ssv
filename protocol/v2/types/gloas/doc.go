// Package gloas holds the node-side wire types introduced by ePBS (EIP-7732 / Gloas), per
// SIP ssvlabs/SIPs#94 — the payload-attestation containers, the PTC duty, and the Gloas
// BeaconVote variant. The associated roles, domains, and partial-signature types live in
// ssv-spec (spectypes); only types ssv-spec does not yet carry are defined here.
package gloas
