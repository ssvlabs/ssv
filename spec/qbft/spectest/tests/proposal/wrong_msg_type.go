package proposal

import "github.com/bloxapp/ssv/spec/qbft/spectest/tests"

// WrongMsgType tests a proposal msg received with the wrong msg type (not a proposal msg type)
func WrongMsgType() *tests.MsgProcessingSpecTest {
	panic("implement")
}
