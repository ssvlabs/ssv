This folder contains a script and configuration to run Differ on `ssv` vs. `ssv-spec`.

### Usage

```bash
cd ./scripts/spec-alignment
./differ.sh
```

### Reviewing Changes

See [Differ's README](/scripts/differ/README.md)
